# production/hiera.yaml

---
version: 5
defaults:
  datadir: data
  data_hash: yaml_data

hierarchy:
 - name: "Per-node data"
   paths:
    - "nodes/%{::trusted.certname}.yaml"
    - "os/%{facts.os.family}.yaml"
    - "common.yaml"
